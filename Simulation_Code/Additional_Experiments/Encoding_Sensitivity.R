# Encoding sensitivity study, Case 3 (Supplementary Table S12).
# Compares polynomial, Helmert, sum-to-zero, and dummy encodings,
# plus random relabellings of the nominal levels (Jaccard overlap).
# Run from this folder:  Rscript Encoding_Sensitivity.R
source("common.R")
run_encoding()
